include_directories(../include/library/tools)
find_package(Qt5 COMPONENTS Core REQUIRED)
file(GLOB FILE_TOOLS "tools/*.cpp")
message("===================Start Load Tools===================")
message(${FILE_TOOLS})
message("=================== End Load Tools ===================")
add_library(Tools ${FILE_TOOLS})
message(${REQUIRED_LIBS_QUALIFIED})
find_package(Qt${QT_VERSION} COMPONENTS ${REQUIRED_LIBS} REQUIRED)
target_link_libraries(Tools ${REQUIRED_LIBS_QUALIFIED})