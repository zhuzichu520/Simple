file(GLOB CPP_FILES "tools/*.cpp")
message(${CPP_FILES})
add_library(Library ${CPP_FILES})
find_package(Qt${QT_VERSION} COMPONENTS ${REQUIRED_LIBS} REQUIRED)
target_link_libraries(Library ${REQUIRED_LIBS_QUALIFIED})